#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    if idx < 0:
        return(my_list)
    
    ele = len(my_list)

    if idx > ele - 1:
        return(my_list)
    
    my_list[idx] = span

    return (my_list)
